<% if @user == current_user %>
  window.location.href = "<%= root_url %>"
<% else %>
  $('#myModal').modal('hide');
  $.gritter.add({
    title: '提示',
    text: '密码修改成功！'});
  $("#users").DataTable().ajax.reload();
<% end %>

